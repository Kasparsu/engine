Hello World example

This example shows a simple centered rectangle intended to indicate the example is running. If your SDL build exposes `renderDebugText`, the example will attempt to render the text "Hello World" on top; otherwise it draws a colored rectangle.

Run with:

```bash
bun run example hello-world
```
