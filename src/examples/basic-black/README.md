Basic Black Screen example

Files:
- `BasicBlackGame.ts` — minimal `BaseGame` that clears the screen to black each frame.
- `index.ts` — small runner that creates an App, window, renderer and starts the game.

Run (using `bun`):

```bash
bun run src/examples/basic-black/index.ts
```

Or compile/run with your preferred TypeScript runner.
